version: "3.8"
services:
  rabbitmq:
    image: bitnami/rabbitmq:latest
    ports:
      - 5672:5672
      - 15672:15672
    environment:
      - RABBITMQ_USER=user
      - RABBITMQ_PASSWORD=password
      